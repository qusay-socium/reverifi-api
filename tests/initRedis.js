// import Redis from 'ioredis';
// import sinon from 'sinon';

// const connectStub = sinon.stub(Redis.prototype, 'connect').callsFake(() => Promise.resolve());
// const publishStub = sinon.stub(Redis.prototype, 'publish').callsFake(() => Promise.resolve());
// const disconnectStub = sinon.stub(Redis.prototype, 'disconnect').callsFake(() => Promise.resolve());

// global.__stubs = {
//   redis: {
//     connect: connectStub,
//     publish: publishStub,
//     disconnect: disconnectStub,
//   },
// };
